fruits = ["apple", "mango", "banana"]
# fruits.append("apple")
# list.appent() adds a new item "apple" to the list.
# fruits.pop(3)
# for the pop function, i applied the index number to pop the items of the list
new = [0, 9, 4, 6, 7, 3, 4, 9, 7, 3, 5, 8,]
new.sort()
# applied sort to number, we cant mix integers and string to apply sort function.

# remove function removes the exact item we want to remove, for eg"remove mange" removes one mango.
new.remove(4)
print(new)
fruits.sort()

fruits.insert(1, "app")
# inorder to use insert, we use two arguments, like first we give the index and then the name of item we want to add.
print(fruits)
